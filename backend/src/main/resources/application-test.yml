spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: false
      path: /h2-console

  jpa:
    hibernate:
      # in-memory H2 only; production uses Flyway
      ddl-auto: create-drop
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect

  liquibase:
    enabled: false

  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080

logging:
  level:
    com.hrms: DEBUG
    org.springframework: INFO
