-- V8: Demo seed data placeholder
-- Real seed SQL is in db/seed/V8__demo_seed_data.sql (only included with demo profile).
-- Production deploys use classpath:db/migration only; demo data does NOT run.
-- To load demo data: set SPRING_PROFILES_ACTIVE=demo (or add application-demo.yml override).
SELECT 1; -- no-op, keeps Flyway happy with a valid SQL statement
