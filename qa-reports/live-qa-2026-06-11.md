# NU-AURA Live Staging QA — Findings (2026-06-11)
Target: https://hrms-frontend-vert.vercel.app (Railway backend, render profile, demo tenant)

## Issues (for single final fix loop)
- F1 [MED] Employees first-load shows "0 people across 0 departments" + stuck skeletons; reload renders 17. Cold-start/auth race or missing retry/refetch on initial fetch. API itself returns 17 (200).
- F2 [MED/UX] Sidebar scroll not isolated — whole page scrolls instead of just the sidebar nav container (user-reported). Needs height-constrained overflow-y:auto on sidebar nav.

## API RBAC matrix (confirmed correct enforcement)
- /employees: ESS→403, all others→200 ✅
- /payroll/runs, /loans, /travel: only SUPER_ADMIN+HR_MANAGER→200, rest→403 ✅
- /admin/feature-flags: only SUPER_ADMIN→200 ✅
- /expenses /assets /announcements /helpdesk/tickets /holidays /surveys: 200 all roles ✅
- F3 [LOW-verify] /letters: HR_MANAGER→403 (only SUPER_ADMIN 200). Skill says HRA should have letter access — possible RBAC perm gap (letters:read missing from HR_MANAGER).
- NOTE: many 404s in probe = wrong path guesses, not bugs (real paths differ, e.g. leave/payslips).

## Root causes confirmed
- F2 ROOT CAUSE: AppLayout.tsx:326 shell root uses `min-h-[100dvh]` (grows with content) instead of `h-[100dvh]`. NavPanel.tsx:187 and main:404 both have `flex-1 overflow-y-auto` but need a viewport-bounded parent. FIX: min-h-[100dvh] -> h-[100dvh] (keep overflow-hidden). HIGH confidence, 1-line.

## Visual sweep (SUPER_ADMIN, all clean, 0 console errors)
- /me/dashboard, /employees, /leave, /payroll, /recruitment(NU-Hire), /performance(NU-Grow), /fluence/wiki(NU-Fluence): all render, app-aware sidebar switches per sub-app, editorial heroes, proper empty states, dark theme polished. NU-Fluence actually has working UI (not just Phase-2 stub).
- Seed-data gaps (not bugs): leave balances empty 2026, payroll 0 runs, dashboard 0% attendance.

## Additional tests (user-requested)
- Concurrent/multiple logins: 3 simultaneous sessions for same account ALL valid (/auth/me 200 x3). Platform allows multi-session (reasonable HRMS policy). NOTE: no single-session enforcement / "logged out elsewhere" — informational, not a bug.
- F4 [MED] No global offline/network-loss UX. grep: navigator.onLine/offline listener only in generated mobile-sync + biometric code, NOT a global app banner. Skill UIUX-6 expects "No connection" banner + auto-refresh on reconnect. When network drops, API calls fail without a clear offline message.
- ESS RBAC frontend: /payroll as EMPLOYEE -> redirect to /me/dashboard?denied=1 (graceful, no white screen) ✅. Sidebar role-scoped (no Employees list for ESS) ✅.

## FIX APPLIED
- F2 FIXED: AppLayout.tsx min-h-[100dvh] -> h-[100dvh]. Deploying to Vercel prod.
