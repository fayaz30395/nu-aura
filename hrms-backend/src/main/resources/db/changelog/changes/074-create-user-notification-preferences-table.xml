<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="074-create-user-notification-preferences-table" author="system">
        <createTable tableName="user_notification_preferences">
            <column name="id" type="uuid">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="user_id" type="uuid">
                <constraints nullable="false"/>
            </column>
            <column name="tenant_id" type="uuid">
                <constraints nullable="false"/>
            </column>
            <column name="email_notifications" type="boolean" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>
            <column name="push_notifications" type="boolean" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>
            <column name="sms_notifications" type="boolean" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
            <column name="security_alerts" type="boolean" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="timestamp" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="timestamp" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Add foreign key constraint -->
        <addForeignKeyConstraint
                baseTableName="user_notification_preferences"
                baseColumnNames="user_id"
                constraintName="fk_notification_prefs_user"
                referencedTableName="users"
                referencedColumnNames="id"
                onDelete="CASCADE"/>

        <!-- Add foreign key constraint for tenant -->
        <addForeignKeyConstraint
                baseTableName="user_notification_preferences"
                baseColumnNames="tenant_id"
                constraintName="fk_notification_prefs_tenant"
                referencedTableName="tenants"
                referencedColumnNames="id"
                onDelete="CASCADE"/>

        <!-- Add unique constraint on user_id and tenant_id -->
        <addUniqueConstraint
                tableName="user_notification_preferences"
                columnNames="user_id, tenant_id"
                constraintName="uk_notification_prefs_user_tenant"/>

        <!-- Add indexes -->
        <createIndex tableName="user_notification_preferences" indexName="idx_notification_prefs_user">
            <column name="user_id"/>
        </createIndex>

        <createIndex tableName="user_notification_preferences" indexName="idx_notification_prefs_tenant">
            <column name="tenant_id"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
