# Tech Debt

- Add Mockito inline mock-maker agent configuration to remove dynamic agent warnings during tests.
- Fix backend test compilation failures after RBAC/RoleScope changes (tests currently out of sync).
