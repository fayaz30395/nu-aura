# Release 2026-06-04 — Docs & Observability Reconciliation

**Author:** release/docs engineer (subagent 05)
**Date:** 2026-06-04
**Scope:** EDIT only `docs/`, root `README.md` / `MEMORY.md` / `CHANGELOG.md`. No `backend/` or `frontend/` source touched.

## Ground Truth (verified this checkout)

| Fact | Verified value | Evidence |
|------|---------------|----------|
| Latest Flyway migration | **V269** | `backend/src/main/resources/db/migration/V269__allow_tenant_sequence_allocators_under_rls.sql` (highest numbered file) |
| Backend runtime | **Java 21** | `backend/pom.xml:36` → `<java.version>21</java.version>` |
| Spring Boot | 3.4.1 | per `.claude/CLAUDE.md` locked stack; consistent across docs |
| Frontend | Next.js 14 | consistent across docs |
| Seed migration | V171 = `V171__seed_data.sql` | `CHANGELOG.md:119` (rename from `db/seed/V001`) |

Older docs variously claim V100 / V101 / V253 / V171 and "Java 17"; these are reconciled below.

---

## 1. Stale-Reference Sweep

### 1a. Fixed (clearly-wrong, low-risk, current-state claims)

| File:Line | Was | Now | Rationale |
|-----------|-----|-----|-----------|
| `docs/agents/overview.md:38` | Spring Boot 3.4.1 (Java 17), monolith | Java 21 | Runtime is Java 21 |
| `docs/agents/overview.md:83,134,182,236,277` | Spring Boot 3.4.1 (Java 17), package root | Java 21 (×5) | Agent-context current-state reference |
| `docs/architecture/skills.md:11` | `\| Java 17 \| Expert \|` | Java 21 | Current capability table |
| `docs/architecture/backend.md:7` | "on Java 17 (Docker images now Java 21 — see `fd1c677d`)" | "on Java 21 (`<java.version>21</java.version>` in `backend/pom.xml`; Docker images on Java 21 — see `fd1c677d`)" | Removed the contradictory "runtime is 17 but Docker is 21"; pom is authoritative |
| `README.md:45` | `# Flyway (V0–V171)` | `# Flyway (V0–V269)` | Current-state structure map |
| `README.md:219` | `Flyway only (V0–V171, 162+ files)` | `Flyway only (V0–V269)` | Dropped stale file count rather than guess (range V0–V269 has gaps); version range corrected |

Total fixed: **9 references across 5 files.**

### 1b. Left as-is (dated/historical reports or ambiguous — flagged, not edited)

| File:Line | Stale token | Why left |
|-----------|-------------|----------|
| `docs/architecture/technical-baseline.md:5` | "Java 17" | Dated point-in-time report ("Generated: 2026-03-19, Phase 0"). Editing rewrites a historical baseline. |
| `docs/architecture/architecture-analysis.md:83,877` | "Java 17" | Dated analysis doc; describes state at time of writing. |
| `docs/architecture/backend.md:9` | "144 Flyway migrations (V0–V154)" | Stale count/range but part of a snapshot sentence; see "Remaining" below — recommend a refresh in a docs pass, not a blind number swap. |
| `docs/superpowers/plans/2026-04-01-platform-readiness-rbac-features.md:16` | "Spring Boot 3.4 (Java 17)" | Dated plan document (filename carries date). |
| `docs/superpowers/state/QA_RESULTS/migration-chain.md:16,25` | "V180 through V253" | Stale QA-state snapshot referencing an older untracked range. State file, regenerated by tooling. |
| `docs/superpowers/state/TASKS.md:23` | "V180-V253 migration chain" | Task acceptance text from an earlier wave; rewriting changes task history. |
| `docs/audit/release-readiness-100-note-2026-05-24.md:11,14,60` | "V180 through V253", "v253", dirty-worktree narrative | **Confirmed stale** — task brief states tree is now clean on main at V269. This is a dated audit note; superseded by THIS report rather than edited in place. |
| `docs/audit/spring-boot-3.4-to-3.5-upgrade-precheck.md:142` | "Flyway 11 dropped Java 17 support (we're on 21 — fine)" | Actually CORRECT — explicitly states "we're on 21". No fix needed. |
| `README.md:171` | "After seed data (V171 migration)" | **CORRECT** — `CHANGELOG.md:119` confirms `V171__seed_data.sql` is the seed migration. Not a "latest-version" claim. Left intentionally. |
| `MEMORY.md:400,408,430,448,562,786,850-861` | "V0–V91", "V0–V146 / V147–V149", "V67–V91", "V61", "V67", "V81" | MEMORY.md self-declares (lines 3–6) as a living wiki with dated change-log entries (Wave 12-18, etc.). These are historical snapshots, not current-state claims. Recommend an explicit "Current migration head: V269" line rather than rewriting history. |

No `V100` / `V101` literals were found in `docs/`, `README.md`, or `MEMORY.md` — those appear only in the older brief framing; the live stale ranges are V253 (audit/state notes) and V171/V154/V146/V91 (README/MEMORY).

---

## 2. Observability Wiring (read-only — CONFIRMED WIRED, no gaps)

| Concern | Status | Evidence |
|---------|--------|----------|
| Actuator exposure | health, info, metrics, prometheus | `backend/src/main/resources/application-prod.yml:133-138` |
| Health detail hardening | never-when-authorized in prod | `application-prod.yml:140-141` |
| Liveness probe group | configured (ping only, no diskSpace) | `application-prod.yml:148-153` |
| Readiness probe group | configured (kafka excluded on Render) | `application-prod.yml:157-159` |
| Prometheus endpoint + token | `/actuator/prometheus`, scrape-token guarded | `application-prod.yml:159,176-180,205` |
| Metrics tags (region) | configured | `application-prod.yml:180-183` |
| K8s probes | liveness/readiness/startup all → `/actuator/health/...` | `infra/deployment/kubernetes/backend-deployment.yaml:390-410` |
| K8s scrape annotations | `prometheus.io/scrape/port/path` | `backend-deployment.yaml:28-30` |
| Render health check | `healthCheckPath: /actuator/health/readiness` + `PROMETHEUS_SCRAPE_TOKEN` | `render.yaml:51,87` |
| docker-compose.prod healthcheck | backend → `/actuator/health/readiness`; DB/ES/redis healthchecks + `service_healthy` gates | `docker-compose.prod.yml:18,33,84,105-106,129,155-156,183` |
| Prometheus alert rules | present | `infra/monitoring/prometheus/rules/hrms-alerts.yml`, `hrms-slo-alerts.yml`, `nu-aura.rules.yml` |
| Grafana dashboards | api-metrics, business-metrics, overview, webhooks, nu-aura-api + provisioning | `infra/monitoring/grafana/**` |
| Alertmanager | configured | `infra/monitoring/alertmanager/alertmanager.yml` |

**Gaps:** None blocking. Note: K8s manifests live under `infra/deployment/kubernetes/` and `infra/deployment/helm/hrms/` (not a top-level `k8s/`); any doc that points readers at `k8s/` would be a path drift — none found in this sweep, but worth a glance in a future pass.

---

## 3. Runbook Coverage (CONFIRMED COMPLETE)

All six required runbooks exist under `docs/runbooks/`:

| Required | File |
|----------|------|
| deployment | `docs/runbooks/deployment.md` |
| rollback | `docs/runbooks/rollback.md` |
| backup-restore | `docs/runbooks/backup-restore.md` |
| disaster-recovery | `docs/runbooks/disaster-recovery.md` |
| incident-response | `docs/runbooks/incident-response.md` |
| key-rotation | `docs/runbooks/key-rotation.md` |

Bonus runbooks present: `dr-drill-checklist.md`, `data-correction.md`, `payroll-correction.md`, `kafka-dead-letter.md`, `tenant-lifecycle.md`, `ci-workflows.md`, plus dev/QA guides. No referenced-but-missing runbook detected.

---

## 4. Release / Deploy Checklist (CONFIRMED)

- **Pre-deploy checklist:** `docs/runbooks/deployment.md:24` — "## 1. Pre-Flight Checklist" with "Any RED item is a blocker."
- **Rollback triggers:** `docs/runbooks/deployment.md:233` — "## 8. Rollback Triggers" with a Trigger/Threshold/Where-to-check table (`:237`).
- **Rollback runbook triggers:** `docs/runbooks/rollback.md:19` — Trigger→Action table; postmortem template cites trigger reasons (p95 latency / error rate / migration failure) at `:326,335`.
- **Other checklists:** `docs/runbooks/dr-drill-checklist.md`, `docs/runbooks/backend-developer-checklist.md`, `docs/security/scan-checklist.md`.

**Gaps:** None. Deploy checklist + rollback triggers with thresholds are documented.

---

## 5. Remaining for Full Doc Consistency (recommendations, not done here)

1. **Refresh dated baselines deliberately** (not blind number swaps): `docs/architecture/technical-baseline.md`, `architecture-analysis.md`, and `backend.md:9` ("V0–V154", controller/service/entity counts) should be regenerated with a fresh count pass and re-dated, or marked "as of <date>".
2. **MEMORY.md current head line:** add an explicit "Current Flyway head: V269 (clean on main, 2026-06-04)" near the top so the living wiki's historical wave-counts aren't mistaken for current state.
3. **Supersede the 2026-05-24 release-readiness note:** `docs/audit/release-readiness-100-note-2026-05-24.md` describes a dirty worktree at V253; add a one-line "Superseded by docs/audit/release-2026-06-04/" banner so future readers don't act on its stale V253/dirty-tree claims.
4. **State files** (`docs/superpowers/state/*`) carry V180–V253 ranges — these are tool-regenerated; let the tooling refresh them rather than hand-edit.
