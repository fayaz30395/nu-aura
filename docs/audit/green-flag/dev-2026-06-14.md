# DEV Agent — Production Green-Flag Audit (DEPLOYED stack run)

**Run:** 2026-06-14 · **Agent:** dev · **Scope:** architecture, FE↔BE API contracts, data-model/migration integrity, implementation gaps · **Target env:** DEPLOYED (Vercel frontend `https://hrms-frontend-vert.vercel.app` + Railway backend)

## Executive summary

The code-level contract layer is **solid**. All prior DEV-2..DEV-6 FE↔BE endpoint mismatches were re-verified line-by-line and remain fixed or correctly feature-flagged (no dead 404 pages reachable from code). Flyway chain is monotonic (latest **V288**), no duplicate version prefixes, two newest migrations (V287/V288) are idempotent and self-guarding.

The material findings this run come from the **deployed-stack smoke artifact** (`frontend/e2e/remote-deployed-route-smoke.json`, generated 2026-06-11 against Vercel+Railway): SUPER_ADMIN and EMPLOYEE logins FAILED on the live frontend, and an authenticated MANAGER session got **HTTP 404** for `/auth/me` and dashboard-summary with 736 API errors. The backend *code* defines `/auth/me` (AuthController:94), so this is a **deployment/runtime/env-wiring break, not a code contract bug** — owned by the release agent, but it is the single biggest blocker to a DEPLOYED go.

Counts: **0 CRITICAL · 2 HIGH · 3 MEDIUM · 2 LOW** (the 2 HIGH are an env/deploy blocker and a previously-tracked statutory gap; no new code-level contract mismatches found).

## Verification of prior DEV findings (all intact)

| Prior | Verdict | Evidence |
|-------|---------|----------|
| DEV-2 survey | FIXED | `/survey-management` now exists (`SurveyManagementController.java:23`); `grow/survey.service.ts` BASE_URL matches |
| DEV-3 spotlight | FIXED (gated) | `SPOTLIGHT_API_AVAILABLE=false` (`spotlight.service.ts:26`); widget/page/hooks all gate on it (CompanySpotlight.tsx:57,70; useSpotlight.ts:25,38; company-spotlight/page.tsx:107) |
| DEV-4 utilization | FIXED (gated) | per-employee routes repointed to `/project-timesheets/reports/...` (verified `ProjectTimesheetController.java:280-327`); aggregate routes gated by `UTILIZATION_DASHBOARD_API_AVAILABLE=false` |
| DEV-5 loans | FIXED | `loan.service.ts` uses `/repayment`,`/cancel`,`/approve`,`/disburse`,`/activate`,`/pending` — all present on `LoanController.java` |
| DEV-6 helpdesk KB | FIXED (gated) | `KNOWLEDGE_BASE_API_AVAILABLE=false` (`useKnowledgeBase.ts:28`) gates the query; no `/helpdesk/knowledge-base` BE route (HelpdeskController is tickets/SLA only) |
| PROD-1 payroll detail | FIXED | `/payroll/runs/[id]` page + `getPayrollRunById` → `GET /payroll/runs/{id}` (PayrollController) present |
| PROD-4 US/UK statutory | FIXED | IN-only enforcement holds |

## Issue board

| ID | Severity | Module | Description | Impact | Exact Fix (file:line) | Owner Agent | Status |
|----|----------|--------|-------------|--------|-----------------------|-------------|--------|
| DEV-7 | HIGH | deploy/runtime | Deployed-stack smoke (`frontend/e2e/remote-deployed-route-smoke.json:6-36`) shows SUPER_ADMIN (fayaz.m@nulogic.io) and EMPLOYEE (saran@nulogic.io) logins FAIL on Vercel (`loginOk:false`, never leaves `/auth/login`); authenticated MANAGER session gets `/auth/me`=404 + dashboardSummary=404 with 736 apiErrors/62 console errors. Backend code DOES define the route (`AuthController.java:94 @GetMapping("/me")` under `/api/v1/auth`), so this is an env/wiring break (wrong `NEXT_PUBLIC_API_URL`, stale/cold Railway build, or seeded-account gating), NOT a code contract mismatch. | Most roles cannot use the deployed app; a NO-GO for DEPLOYED until resolved | Release agent to verify: (1) Vercel `NEXT_PUBLIC_API_URL` points to the live Railway `/api/v1` base; (2) Railway backend is on the fixed/current build and not cold-starting to 404; (3) `DEMO_CREDENTIALS_ENABLED` state for the seeded SUPER_ADMIN/EMPLOYEE accounts (QA-1 prod gate). Re-run the smoke after fix. Artifact: `frontend/e2e/remote-deployed-route-smoke.json` | release | Open |
| DEV-8 | HIGH | payroll/statutory | LWF `calculateForPayrollRun` is still NOT implemented: fail-closed `throw new UnsupportedOperationException` when `app.features.lwf=false` (`LWFService.java:297-300`), and even when the flag is true it returns `Collections.emptyList()` (`LWFService.java:330`) — the per-employee deduction insert was never written. Reachable via `POST /api/v1/payroll/lwf/calculate` (`LWFController.java:135-143`). | IN-tenant LWF remittance is empty; statutory compliance gap. Fail-closed default keeps non-IN deploys safe and surfaces the gap loudly. Re-confirms prior PROD-3 (still Open). | Implement per-employee iteration + deduction insert in `LWFService.calculateForPayrollRun` (`LWFService.java:296-331`), OR formally descope LWF for this release and keep `app.features.lwf=false` documented as a known IN gap. | release / fix-payroll | Open |
| DEV-9 | MEDIUM | reports/frontend | `useExportUtilizationReport()` (`frontend/lib/hooks/queries/useReports.ts:186-196`) → `utilizationService.exportReport` → `GET /reports/export`, a route with NO backend mapping, and the hook is NOT gated by `UTILIZATION_DASHBOARD_API_AVAILABLE` (unlike the dashboard/all-employees hooks at :109,:139). Confirmed dead: no component/page imports the hook. | None today (dead code, zero consumers); risk of a future 404 if a page wires it up. Matches prior DEFER note. | Either gate it `enabled: UTILIZATION_DASHBOARD_API_AVAILABLE` / guard the mutation, or delete `useExportUtilizationReport` + `utilizationService.exportReport` (`utilization.service.ts:162`). | release (post-GA) | Open |
| DEV-10 | MEDIUM | helpdesk/frontend | KB *query* `useKnowledgeBaseArticles` is gated (`useKnowledgeBase.ts:55`), but the KB *mutations* `useCreateArticle` (:90, `POST /helpdesk/knowledge-base`), `useArticleFeedback` (:63, `PATCH /helpdesk/knowledge-base/{id}/helpful`) are NOT gated — they target the same non-existent BE routes. The page is gated (`knowledge-base/page.tsx` reads `KNOWLEDGE_BASE_API_AVAILABLE`) so they cannot fire today. | None today (page hidden); a 404 footgun if the page is ever ungated without also implementing the BE. | When the page is ungated, either ship `/helpdesk/knowledge-base` BE endpoints or guard these mutations. Note `useCreateTicketFromKB` correctly uses the real `/helpdesk/tickets` route. | release (post-GA) | Open |
| DEV-11 | MEDIUM | data/legacy-db | V287 documents a real prior data-corruption class: legacy/restored DBs materialized `tax_declarations.sec_80d_parents` as the malformed identifier `"sec d_parents"`, which 500s every `GET /api/v1/tax-declarations` (`Hibernate: column ... does not exist`). The migration is idempotent/self-healing (`V287__...sql:14-31`), but any DB that already ran the corrupting path before V287 needs V287 applied. | Tax-declaration page 500s on un-migrated legacy DBs until V287 runs. Clean V0 applies are unaffected. | Ensure V287 is in the deployed Flyway chain (it is at HEAD); add to deploy checklist a verify step that `tax_declarations.sec_80d_parents` exists post-migrate. | release | Open |
| DEV-12 | LOW | migrations | Flyway version numbers are non-contiguous (gaps V273-276, V278-281) but strictly monotonic with no duplicate prefixes — Flyway tolerates gaps, so this is cosmetic. Latest applied is V288. | None (informational). | No action; document expected latest = V288 in deploy checklist. | release | Open |
| DEV-13 | LOW | reports/frontend | Aggregate utilization endpoints (`/time-tracking/reports/dashboard`, `/all`, `/reports/export`) remain unimplemented on the BE; FE correctly hides the `/reports/utilization` page behind `UTILIZATION_DASHBOARD_API_AVAILABLE=false`. Tracked here only so it is not mistaken for a regression. | None (page hidden). | Implement the aggregate report endpoints or leave gated; no blocker. | release (post-GA) | Open |

## Architecture / contract health checks (passed)

- **Payroll contracts**: every `payroll.service.ts` path (runs, payslips incl. `/payslips/employee/{id}/period/pdf` PayrollController:350, components incl. `/components/evaluate` :565 and `/components/recompute-order` :574, salary-structures) resolves to a real `PayrollController` mapping. No mismatch.
- **Current-user**: `/auth/me` (AuthController:94), `/users/me` (UserController:36), `/employees/me` (EmployeeController:124) all exist; FE `auth.ts:45` and `employee.service.ts:25,77` align. (Deployed 404 is runtime, see DEV-7.)
- **Migrations**: 277 files, latest V288, no duplicate version prefixes; V287/V288 idempotent and RLS-aware (V288 per-tenant NOT-EXISTS guarded insert).
- **Transactional integrity**: notification services (recent multi-commit fix) carry `@Transactional`; the `REQUIRES_NEW` change for AFTER_COMMIT listener persistence is a correct pattern. No missing-`@Transactional` multi-write found in the changed set.
- **Pagination**: employee/loan/scheduled-report list endpoints return paged responses (`Page<T>` / `PageResponse`), not unbounded `List<T>`.
- **TypeScript**: changed FE services define proper types (no `any` introduced in the audited fix-wave files); React Query + RHF+Zod conventions honored (KB page uses `zodResolver`, hooks use `useQuery`/`useMutation`).

## Note on severity vs. compressed-scope rule

Only DEV-7 is a true DEPLOYED blocker, and it is an environment/wiring problem rather than a code defect — no code change in this repo will fix it; the release agent must correct deploy config and re-run the smoke. DEV-8 (LWF) is a real but already-tracked statutory gap that is fail-closed safe for non-IN tenants. Everything else is hidden behind feature flags or is dead code.
