# Product / BA Green-Flag Audit — 2026-06-14

Agent: `product-ba` | Repo: `/Users/fayaz.m/IdeaProjects/nulogic/nu-aura` | Scope: business completeness, end-to-end journeys, dead-end navigation, P0 validations, status of prior OPEN items (PROD-3, BA-5b, TEST-1).

**Method:** (1) re-read `ISSUE_BOARD.md`, `PRODUCT.md`, `docs/audit/green-flag/product.md`; (2) enumerated all `frontend/app/**/page.tsx` routes; (3) extracted every `router.push` / `router.replace` / `href="/..."` target across `frontend/app/**` and cross-checked each against existing pages + `next.config.js` rewrites/redirects (none defined for app routes) + the absence of a `frontend/middleware.ts` app-route rewrite; (4) verified backend reality for PROD-3 (LWF), BA-5b (expense/loan orphan guard), and the live LWF payslip path; (5) checked P0 form validations (leave apply) and module landing CTAs.

## Status of prior OPEN items (code reality 2026-06-14)

- **PROD-1 (payroll run detail 404) — RESOLVED.** `frontend/app/payroll/runs/[id]/page.tsx` now exists; board marks Closed. Confirmed.
- **BA-5b (expense/loan direct-approve orphans workflow) — RESOLVED (board still says Open/MEDIUM).** Both direct-approve paths now cancel the live execution in the same transaction: `ExpenseClaimService.approveExpenseClaim` calls `workflowService.cancelActiveExecutionForEntity(EXPENSE_CLAIM, claimId, ...)` (`ExpenseClaimService.java:213-217`); `LoanService.approveLoan` calls the same for the loan entity (`LoanService.java:94-96`). The leave-path guard pattern has been replicated. **Recommend closing BA-5b on the board.**
- **PROD-3 (LWF bulk computation unimplemented) — STILL OPEN.** `LWFService.calculateForPayrollRun` still throws `UnsupportedOperationException` (`LWFService.java:296-300`); flag `app.features.lwf` defaults `false` (`LWFService.java:76`); reachable via `POST /api/v1/lwf/calculate` → 501 (`LWFController.java:135-139`). See PROD-3 below for precise user-facing impact.
- **TEST-1 (e2e suite debt) — STILL OPEN.** Per board (line 66), leave specs still assert removed modal UI, `use-cases.yaml` missing, vacuous `|| true` assertions, plans/runs unseeded. No evidence of a rewrite landing since 2026-06-11. Carries forward as post-GA test debt.

## Issues

| ID | Severity | Module | Description | Impact | Exact Fix (file/route) | Owner Agent | Status |
|----|----------|--------|-------------|--------|------------------------|-------------|--------|
| BA-9 | HIGH | learning (LMS) | Learning module landing `/learning` (sidebar entry `menuSections.tsx:486,694`, visible to all tenants) has its three primary CTAs pointing to routes that do not exist: `href="/learning/catalog"` (`frontend/app/learning/page.tsx:139,226,235`) and `href="/learning/my-courses"` (`:241,404,487`). No `learning/catalog/page.tsx` or `learning/my-courses/page.tsx` exists — the real catalog is `frontend/app/learning/courses/page.tsx`. Hero fallback also uses singular `/learning/course/${id}` (`:225`) vs the real `/learning/courses/[id]`. | Every "Browse Catalog" / "My Courses" / hero CTA on the LMS landing 404s. Learners cannot reach the course catalog or their enrollments from the module home — the LMS journey dead-ends at its front door. | Repoint `/learning/catalog` → `/learning/courses` and add a `/learning/my-courses` page (or repoint to `/training/my-learning` / `/learning/courses?filter=enrolled`); fix singular `/learning/course/` → `/learning/courses/` at `learning/page.tsx:225`. | dev | Open |
| BA-10 | HIGH | admin / navigation | 9 admin pages redirect non-admin users to `router.push('/home')` on the permission-denied branch, but `/home` is not a route (no `app/home/page.tsx`, no rewrite in `next.config.js`, no `middleware.ts`). Files: `admin/leave-types/page.tsx:97`, `admin/implicit-roles`, `admin/permissions`, `admin/integrations`, `admin/office-locations`, `admin/holidays`, `admin/roles`, `admin/settings`, `admin/leave-requests` (all `router.push('/home')`). | Any non-admin who lands on an admin URL is bounced to a non-existent route → hits `not-found.tsx` instead of a usable dashboard. Confusing dead-end on the RBAC denial path for the most common low-privilege users. | Replace `router.push('/home')` with `router.push('/me/dashboard')` (the canonical authenticated landing used everywhere else) in all 9 files; or add a `/home` redirect. | dev | Open |
| PROD-3 | HIGH | payroll / statutory | LWF bulk computation still unimplemented: `LWFService.calculateForPayrollRun` throws `UnsupportedOperationException` (`LWFService.java:296-300`), flag `app.features.lwf=false` default (`:76`), exposed at `POST /api/v1/lwf/calculate` (`LWFController.java:135`). Per-employee LWF *is* computed into payslips (`StatutoryDeductionService.java:175-177`), but `lwf_deductions` rows are never persisted for a run, so the LWF remittance report (`GET /api/v1/lwf/report` → `getRemittanceReport`, surfaced on `/lwf` page "Generate Report") reads an empty table. | India statutory compliance gap: LWF deducted from employees but no remittance dataset to file. `/lwf` remittance report always shows zero employees/amounts for any period. (The `/lwf` page itself does NOT call the 501 endpoint, so no user-facing 501 — but the report is silently empty.) | Implement per-employee iteration + `LWFDeduction` insert in `calculateForPayrollRun`, wire into the payroll run pipeline, set `app.features.lwf=true` for IN. If not shippable: formally descope, hide the `/lwf` remittance "Generate Report" UI, and document the manual remittance process. | dev + release | Open |
| BA-11 | MEDIUM | leave | `/leave/admin/carry-forward/page.tsx` remains an orphan: not in `menuSections.tsx`, not linked from `/leave` (only `/leave/encashment` is linked at `leave/page.tsx`). (Prior PROD-7, still unaddressed.) | Year-end carry-forward operation is unreachable via navigation; balances may not be carried forward at the year boundary because admins cannot find the screen. | Add a child item under the Leave Setup admin section in `menuSections.tsx` (next to leave-types / leave-requests-admin), gated by `LEAVE_TYPE_VIEW` / `LEAVE_VIEW_ALL`. | dev | Open |
| BA-12 | MEDIUM | payroll | `/admin/payroll` still links `/payroll/bulk-processing` as a quick action (`admin/payroll/page.tsx:52`), and that page is a hard-coded "Coming Soon" stub (`BulkProcessingWizard.tsx`, `payroll.service.ts` `BULK_PROCESSING_AVAILABLE=false`). (Prior PROD-6, still present.) | Admin quick-action leads to a stub on a P0 module. Graceful (has redirect) but a visible incompleteness. | Remove the quick action from `admin/payroll/page.tsx:52` until the flag flips, or label it "(coming soon)". | dev | Open |
| BA-13 | LOW | leave | Leave-apply Zod schema (`frontend/app/leave/apply/page.tsx:17-25`) has no `.refine` enforcing `endDate >= startDate` or requiring `halfDayPeriod` when `isHalfDay` is true. Date picker sets `minDate` and backend enforces both (DATA-1 range validation, BA-3 single-day half-day @AssertTrue), so this is defense-in-depth only. | Client can theoretically submit an invalid range / half-day-without-period and rely on a server 400 instead of inline UX. No data-integrity risk (server validates). | Add `.refine(d => d.endDate >= d.startDate, {...})` and `.refine(d => !d.isHalfDay || d.halfDayPeriod, {...})` to the schema for inline errors. | dev | Open |
| TEST-1 | MEDIUM | e2e-suite | (Carried) Leave specs assert removed modal UI, `use-cases.yaml` missing, vacuous `|| true` assertions in rbac-employee-boundaries, plans/runs unseeded. No rewrite landed since 2026-06-11. | E2E suite cannot regression-protect the redesigned leave flow or RBAC denials. | Rewrite leave specs against `/leave/apply` + `/leave/approvals`; restore `use-cases.yaml`; replace vacuous assertions; seed plans/runs. | release (post-GA) | Open |

## End-to-end journey verification

- **hire → onboard:** `/recruitment/candidates/[id]/offer` exists; onboarding `/onboarding/new`, `/onboarding/templates` exist and are linked. No dead step found. PASS.
- **leave apply → approve → balance:** `/leave/apply` (Zod + RHF, posts to `LeaveRequestController`), `/leave/approvals` inbox, balance release on reject/approve (BA-2/BA-4 fixed) all present. PASS (validation hardening = BA-13 only).
- **payroll run → payslip:** `/payroll/runs` + `/payroll/runs/[id]` detail (PROD-1 fixed) + `/payroll/payslips` + `/me/payslips`. PASS. LWF remittance sub-journey blocked = PROD-3.
- **expense submit → approve → reimburse:** `/expenses`, `/expenses/approvals`, direct-approve now cancels orphan workflow (BA-5b fixed). PASS.
- **learning enroll → complete:** BROKEN at the front door — `/learning` landing CTAs 404 (BA-9). Quiz completion also still blocked per prior PROD-8 (quiz player stub). FAIL.

## Top must-fix list for production

1. **BA-9 (HIGH)** — repoint `/learning` landing CTAs (`/learning/catalog`, `/learning/my-courses`, singular `/learning/course/`). LMS module is unusable from its own home page.
2. **BA-10 (HIGH)** — fix `/home` non-route redirect in 9 admin pages → `/me/dashboard`. Every non-admin hitting an admin URL dead-ends.
3. **PROD-3 (HIGH)** — implement LWF bulk persistence OR formally descope + hide the empty `/lwf` remittance report. India compliance.

## Notes / verified-healthy

- BA-5b is functionally fixed in code (board wording lags). Recommend board update.
- Per-employee LWF deduction is correctly applied in live payslips; only the bulk remittance dataset is the gap.
- Other prior PROD-9..PROD-19 stubs (payments webhooks, calendar sync, mobile API, Keka import, Gantt, predictive-analytics export) are feature-flag-gated or non-P0; carry forward as previously logged.
