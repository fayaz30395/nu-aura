# DEV Agent — Architecture & Implementation Gap Audit (Green-Flag)

**Date:** 2026-06-10 · **Agent:** dev · **Task:** DEV-01
**Scope:** API contract FE↔BE, tenant scoping / soft-delete / indexes, error handling, transaction
boundaries, Wave-10 backlog verification. P0 domains prioritized: employee, leave, attendance,
payroll, approval workflow, auth.
**Method:** static cross-reference of 1,747 backend routes (`com.nulogic.api.**`) against 961
frontend `apiClient` calls (`frontend/lib/services/**`, `frontend/lib/hooks/**`), plus targeted
code reads. Every claim verified in code today.

---

## Findings

| ID | Severity | Module | Description | Impact | Exact Fix | Owner Agent | Status |
|----|----------|--------|-------------|--------|-----------|-------------|--------|
| DEV-1 | HIGH | Approval Workflow | `WorkflowService.processApprovalAction` (`backend/src/main/java/com/nulogic/application/workflow/service/WorkflowService.java:638`) guards double-approval with read-then-write status checks only. `WorkflowExecution` (`domain/workflow/WorkflowExecution.java:35`) and `StepExecution` have **no `@Version`** and the load uses `workflowExecutionRepository.findByIdAndTenantId` with **no `@Lock(PESSIMISTIC_WRITE)`** (zero `@Lock`/`FOR UPDATE` in `infrastructure/workflow/`). Two concurrent APPROVE requests on the same step both pass the `PENDING` check and both commit. | Double step-approval → `invokeCallback` (line 792) fires twice → duplicate side effects on `onApproved` handlers (expense payout, loan disbursal trigger, leave finalization). Data-integrity risk under concurrent approvers. | Mirror the payroll pattern (`PayrollRunService.getPayrollRunForUpdate`, line 279): add `findByIdAndTenantIdForUpdate` with `@Lock(LockModeType.PESSIMISTIC_WRITE)` to `WorkflowExecutionRepository` and use it in `processApprovalAction`; additionally add `@Version private Long version;` to `WorkflowExecution` and `StepExecution` (+ Flyway `ALTER TABLE ... ADD COLUMN version BIGINT NOT NULL DEFAULT 0`). | dev | Open |
| DEV-2 | HIGH | Survey | FE/BE path mismatch: `frontend/lib/hooks/queries/useSurveyQuestions.ts` calls `GET/POST /survey-management/{id}/questions` (:29,:71), `GET /survey-management/{id}/analytics` (:43), `GET /survey-management/templates` (:56), `DELETE .../questions/{qid}` (:101), `POST /survey-management/submit` (:128), `POST /survey-management/{id}/clone` (:159). Backend `SurveyManagementController` (`api/survey/controller/SurveyManagementController.java:23`, base `/api/v1/survey-management`) has **none of these**; questions live at `SurveyAnalyticsController` base `/api/v1/survey-analytics` → `/surveys/{surveyId}/questions` (:37,:47). | Live pages `app/surveys/[id]/page.tsx`, `app/surveys/[id]/respond/page.tsx`, `app/surveys/[id]/analytics/page.tsx` get 404s — survey authoring, responding and analytics are broken end-to-end. | Repoint `useSurveyQuestions.ts` to `/survey-analytics/surveys/${surveyId}/questions` (GET/POST) and add the missing backend endpoints (`templates`, `submit`, `clone`, `analytics`, `DELETE question`) to `SurveyManagementController`/`SurveyAnalyticsController` — or expose the FE-expected paths as `@PostMapping("/{surveyId}/questions")` etc. on `SurveyManagementController`. Pick ONE canonical contract per `docs/build-kit/11_API_STANDARDS.md`. | dev | Open |
| DEV-3 | HIGH | Dashboard / Spotlight | `frontend/lib/services/platform/spotlight.service.ts` calls `GET /spotlights/active` (:24), `GET/POST /spotlights` (:32,:42), `PUT/DELETE /spotlights/{id}` (:50,:58). **No spotlight controller exists anywhere in `backend/src/main/java`** (`grep -rln spotlight api/` → empty). | `components/dashboard/CompanySpotlight.tsx` (rendered via useQuery on dashboard) and `app/company-spotlight/page.tsx` 404 on every load — visible dashboard widget failure on day 1. | Either implement `SpotlightController` + entity/repo/migration (tenant_id, is_deleted, display_order) under `api/announcement` or `api/engagement`, or remove/feature-flag `CompanySpotlight` from the dashboard and the `/company-spotlight` route until the backend ships. | dev | Open |
| DEV-4 | HIGH | Time Tracking / Reports | `frontend/lib/services/hrms/utilization.service.ts` calls 8 endpoints under `/time-tracking/reports/...` (`employee/{id}/utilization` :30, `/summary` :43, `/weekly` :55, `/monthly` :68, `project/{id}` :81, `dashboard` :98, `utilization`, `export`). Backend `TimeTrackingController` (`api/timetracking/controller/TimeTrackingController.java:26`) exposes only `/summary` (:147) and `/summary/project/{projectId}` (:157) — **no `/reports/*` mappings exist**. | `app/reports/utilization/page.tsx` (+ `lib/hooks/queries/useReports.ts`) is fully broken — utilization reporting dead. | Add a `TimeTrackingReportController` (`@RequestMapping("/api/v1/time-tracking/reports")`) backed by existing summary queries, or rewrite `utilization.service.ts` to consume `/time-tracking/summary` + client-side aggregation. | dev | Open |
| DEV-5 | HIGH | Loans | Contract drift in `frontend/lib/services/hrms/loan.service.ts`: `POST /loans/{id}/payment` (:87) vs backend `POST /loans/{id}/repayment` (`api/loan/controller/LoanController.java:124`); FE `PUT /loans/{id}` (:12), `DELETE /loans/{id}` (:22), `POST /loans/{id}/submit` (:61), `POST /loans/{id}/close` (:95), `GET /loans/summary` (:101) have **no backend mapping** (controller has approve/reject/disburse/activate/repayment/cancel/my/pending/active only). | Loan repayment recording, edit, delete, submit, close and the summary card on `app/loans/page.tsx` / `app/loans/[id]/page.tsx` fail with 404 — core loan lifecycle actions broken in UI. | Rename FE call to `/loans/${id}/repayment`; map FE `close` → backend `cancel` or add `@PostMapping("/{id}/close")`; add `PUT /{id}`, `DELETE /{id}`, `GET /summary` to `LoanController` or remove the dead FE actions. | dev | Open |
| DEV-6 | HIGH | Helpdesk KB | `frontend/lib/hooks/queries/useKnowledgeBase.ts` calls `GET /helpdesk/knowledge-base` (:43), `PATCH /helpdesk/knowledge-base/{id}/helpful` (:60), `POST /helpdesk/knowledge-base` (:87). `HelpdeskController` (`api/helpdesk/controller/HelpdeskController.java:26`) has tickets/comments/categories only — **no knowledge-base endpoints**. | `app/helpdesk/knowledge-base/page.tsx` is dead on arrival (list, create, helpful-vote all 404). | Implement `knowledge-base` endpoints on `HelpdeskController` (entity + repo + V270 migration with `tenant_id`, `is_deleted`) or hide the route/nav entry until implemented. | dev | Open |
| DEV-7 | MEDIUM | Payments | `frontend/lib/services/core/payment.service.ts` calls `GET /payments/config` (:141), `GET /payments/config/{provider}` (:134), `PATCH /payments/config/{provider}/toggle` — backend `PaymentConfigController` (`api/payment/controller/PaymentConfigController.java:21`) has only `POST` (:32) and `POST /test-connection` (:48). Several list methods are stubs already marked "STUB: Backend endpoints not implemented — do not call" (:55-57) yet remain exported. `GET /payments/status/{status}` (:64) collides conceptually with backend `GET /payments/{paymentId}/status`. | `app/payments/config/page.tsx` cannot list or toggle provider configs (404); `app/payments/page.tsx` filter-by-status/type/provider broken if invoked. | Add `GET /`, `GET /{provider}`, `PATCH /{provider}/toggle` to `PaymentConfigController`; delete or throw on the stub methods in `payment.service.ts` so pages can't call them. | dev | Open |
| DEV-8 | MEDIUM | Travel | `frontend/lib/services/hrms/travel.service.ts` calls `GET /travel/requests/employee/{id}` (:58), `GET /travel/requests/status/{status}` (:72), `POST /travel/requests/{id}/complete` (:130), `GET /travel/requests/employee/{id}/summary` (:224) — none mapped in `TravelController` (`api/travel/controller/TravelController.java:24`; has my/pending/upcoming/approve/reject/cancel). | Travel admin views by employee/status and the "complete trip" action 404. | Add the 4 mappings to `TravelController` (employee-scoped list can reuse `findByTenantIdAndEmployeeId`), or repoint FE to `/travel/requests/my` where the viewer is the employee. | dev | Open |
| DEV-9 | MEDIUM | Knowledge (Fluence) | `frontend/lib/services/platform/fluence.service.ts` calls ~11 endpoints with no backend mapping: `GET /knowledge/wiki/spaces/{}/pages/{}`, `GET /knowledge/wiki/pages/{}/revisions`, `POST .../revisions/{}/restore`, `POST /knowledge/templates/instantiate`, `POST/DELETE /fluence/engagement/favorites...`, `GET /knowledge/wiki/pages/my`, `GET /knowledge/blogs/my`, `PUT /knowledge/wiki/pages/{}/editors`, `PUT /knowledge/blogs/{}/editors`. | Wiki revision history/restore, favorites, "my pages/blogs", and editor management silently 404 inside the Fluence UI. | Audit each against `api/knowledge/*` controllers; implement or remove. Revisions/restore is the highest-value (data-loss recovery path). | dev | Open |
| DEV-10 | MEDIUM | Auth | `frontend/lib/hooks/useAuthStatus.ts:32` calls `GET /auth/verify`; `AuthController` (`api/auth/controller/AuthController.java:28`) has `/me` (:94) but no `/verify`. Hook is currently exported via `lib/hooks/index.ts` but not consumed by any page — a latent break: a 404 lands in the non-401 catch (`setIsValid(null)`), so session validity is never confirmable. | Any future consumer of `useAuthStatus` silently never validates sessions (always "unknown"). | One-line FE fix: call `/auth/me` instead, or add `@GetMapping("/verify")` returning `{"valid": true}` to `AuthController`. | dev | Open |
| DEV-11 | MEDIUM | Cross-module contract drift | Remaining unmatched FE→BE calls from the route diff (78 total, deduped): `POST /reports/{}`, `POST /export/{}`, `POST /migration/{}` (`core/importExport.service.ts`), `GET /keka-import/template/{}`, `GET /lms/enrollments/{}`, `POST /review-cycles/{}/complete`, `POST /projects/{}/activate|close`, `GET /projects/export`, `GET /projects/calendar/*`, `GET /projects/gantt/*`, `PUT /pm/tasks/{}/dates|progress`, `PUT /recruitment/agencies/{}/submissions/{}/status`, scorecard endpoints (`hire/scorecard.service.ts` → backend lives at `/recruitment/scorecards`, `ScorecardController.java:24`), `GET /expenses/mileage/*`, `GET /goals/employee/{}`, `GET /reviews/employee/{}`, `GET /employees/{}/skills`, `GET /home/on-leave`, `GET /home/attendance/me`, `GET /wall/praise/employee/{}`, `GET /dashboards/employee*`, SAML config CRUD (`core/saml.service.ts`). Some are dead service methods; several back live pages. | Scattered 404s across secondary modules; each is a broken button/page in prod. | Run the route-diff script (this audit's methodology) in CI as a contract test; for each entry either add the backend mapping or delete the FE method. Triage list above is exhaustive as of today. | dev | Open |
| DEV-12 | LOW | Workflow (FE paths) | `frontend/lib/services/core/workflow.service.ts` calls `/workflow/...` — these DO match `WorkflowController` (`api/workflow/controller/WorkflowController.java:22`, base `/api/v1/workflow`). Verified matched; listed here to pre-empt re-reporting. Approval inbox path `/workflow/inbox` matched. | None — verified working contract. | No action. | — | Closed (verified) |
| DEV-13 | LOW | Tenant scoping | Repositories with id-only JPQL queries (`EmployeeRepository.java:30,37,53,61,73`, `StepExecutionRepository.java:33-103`, `EmployeePayrollRecordRepository.java:141-175`, etc.) rely on callers pre-validating tenant ownership. Spot-checked P0 services: `PayrollRunService` (`:77,:96` use `.filter(pr -> pr.getTenantId().equals(tenantId))`, `:279` uses `findByIdAndTenantIdForUpdate`), `LeaveRequestService.approveLeaveRequest:167` filters tenant — pattern holds. `WorkflowExecutionRepository.countAllPendingCrossTenant` (:62) is intentionally cross-tenant (SuperAdmin overview, documented). `TenantApplicationRepository.findByTenantIdAndApplicationIdUnscoped` (:24) is intentionally `@Where`-bypassing but still tenant-parameterized. | Residual risk only if a new caller forgets the tenant filter. | Add an ArchUnit test: any repository method named `findById*` invoked from `application/**` services must be wrapped by a tenant-filtered variant; flag raw `findById(` on `TenantAware` entities. | qa | Open |
| DEV-14 | LOW | Infra drift | Stale manifest `infra/deployment/kubernetes/backend-deployment.yaml:380-388` still ships `-Xmx1g` with `limits.memory: 1Gi` (zero off-heap headroom — Wave-10 P2-5). CI deploys via Helm (`.github/workflows/deploy.yml:161` uses `infra/deployment/helm/hrms` with `values-prod.yaml:21-27`, which is correctly sized at 2Gi limit), so the raw manifest is dead-but-dangerous. Also Wave-10 P1-4: `infra/deployment/helm/hrms/values.yaml:134-135` default hosts remain `hrms.example.com`. | Anyone applying the raw manifest gets OOMKill-prone pods; default helm install without overrides provisions certs for example.com silently. | Delete or clearly mark `kubernetes/backend-deployment.yaml` as non-canonical; add a `required` template assertion in the chart that `ingress.hosts.frontend != "hrms.example.com"`. | devops/release | Open |
| DEV-15 | LOW | Timezone residue | Wave-10 P0-1 is substantially fixed (`TenantTimeService`/`TenantTimeProvider` referenced in 201 files; **zero** unzoned `now()` left in `application/payroll`, `application/attendance`, `application/leave`). 140 unzoned `LocalDate.now()/LocalDateTime.now()` callsites remain, concentrated in `domain/**` field initializers and `common/util` (e.g. `domain/compliance` 4, `domain/notification` 3, `domain/attendance` 3). No JVM-level TZ pin (`-Duser.timezone` / `TZ`) found in `backend/Dockerfile` or helm env. | Domain-default timestamps (audit-ish columns) can be off by one day on UTC pods around midnight boundaries — low blast radius, not payroll-affecting. | Add `JAVA_TOOL_OPTIONS: -Duser.timezone=Asia/Kolkata` (or UTC + tenant conversion at edges) to helm `values-prod.yaml` env; burn down the 140 residual callsites module-by-module. | dev | Open |

---

## Wave-10 Backlog — verified status in code TODAY

| Wave-10 Item | Status | Evidence |
|---|---|---|
| P0-1 timezone (855 unzoned `now()`) | **MOSTLY FIXED** (residue → DEV-15) | `common/util/TenantTimeService.java` used by 201 files; 0 hits in payroll/attendance/leave; 140 residual (domain init + util); no JVM TZ pin |
| P0-2 `react-hooks/exhaustive-deps` | **FIXED** | `frontend/eslint.config.mjs:91` (`warn` repo-wide), `:147` (`error` escalation) |
| P0-3 `tenants.timezone` column | **FIXED** | `domain/tenant/Tenant.java:54-56` + `db/migration/V165__tenant_timezone.sql` |
| P0-4 LeaveAccrualScheduler cross-pod race | **FIXED** | `application/leave/scheduler/LeaveAccrualScheduler.java:57` `@SchedulerLock(name="accrueMonthlyLeave", lockAtMostFor="PT4H")` + `common/config/ShedLockConfig.java` |
| P0-5 webhook secret rotation | **FIXED** | `api/webhook/controller/WebhookRotationController.java` + `WebhookService.java:300` dual-secret window (`previousSecret` + expiry) |
| P1-1 Kafka idempotency | **FIXED** | `docs/audit/kafka-idempotency-audit.md`: 7/7 listeners SAFE after FluenceSearchConsumer + DeadLetterHandler fixes |
| P1-4 helm hostname placeholder | **OPEN** (→ DEV-14) | `values.yaml:134-135` still `hrms.example.com` |
| P1-5 eager bundle (Tiptap/Recharts/ExcelJS) | **PARTIAL** | 34 `next/dynamic` usages found; not fully verified per-route — defer to release agent |
| P1-6 wall feed N+1 | **FIXED** | `infrastructure/wall/repository/WallPostRepository.java:89` `@EntityGraph(author, praiseRecipient...)`; `PostReactionRepository.java:81-93` `JOIN FETCH` |
| P1-7 `__Host-` cookie prefix | **FIXED** | `common/config/CookieConfig.java` (S10-J) |
| P2-1 soft-delete native-query leaks | **FIXED — all 14 deferred leaks closed** | S13-B sweep: `SOFT_DELETE_GUARD` markers in 9 files; verified `LeaveRequestRepository.java:118-121` (balance math), `EmployeeRepository.java:494-501` (`findDepartmentHeadUserId`), `RoleRepository.java` recursive CTE both legs filtered; full-codebase scan: **zero** native queries missing tenant_id; only intentional `TenantApplicationRepository:24` lacks is_deleted (platform table, tenant-parameterized) |
| P2-3 login captcha | **ADDRESSED** | captcha field in `api/auth/dto/LoginRequest.java` + handling in `application/auth/service/AuthService.java` |
| P2-4 notification delivery_status index | **ADDRESSED** | `db/migration/V169__dashboard_partial_indexes.sql` |
| P2-5 JVM heap headroom | **FIXED in canonical path / drift remains** (→ DEV-14) | `helm/hrms/values-prod.yaml:21-29` 2Gi limit + headroom comment; stale `kubernetes/backend-deployment.yaml:380` unchanged |
| P2-6 graceful shutdown | **FIXED** | `application.yml:16` `timeout-per-shutdown-phase: 30s` + `server.shutdown` (lines ~273) |

## Other areas verified clean (no findings)

- **Error handling:** `common/exception/GlobalExceptionHandler.java` — catch-all at `:757` returns sanitized "An unexpected error occurred"; `application.yml:280-282` `include-stacktrace: never`, prod profile `include-message: never` (`application-prod.yml:195-196`); the `include-message: always` at `application.yml:544` is inside the `on-profile: dev` document (line 521) — safe.
- **Payroll transactions:** `PayrollRunService` is the reference implementation — `REPEATABLE_READ` on every state transition, pessimistic `findByIdAndTenantIdForUpdate`, `PayrollPeriodLock` advisory lock serializing sync + async (Kafka) paths (T1-04), pre-flight salary-structure coverage check, state guards in domain (`PayrollRun.approve` R2-011).
- **Leave approval money-math:** `LeaveBalance` carries `@Version` (R2-001); `approveLeaveRequest` validates balance before deduct (BIZ-003); notifications deferred to AFTER_COMMIT.
- **Hot-path indexes:** composite indexes present — `idx_attendance_records_tenant_employee_date`, `idx_leave_requests_tenant_emp_status`/`_tenant_dates`/`_pending`, `idx_step_executions_inbox`/`_pending_deadline`, `idx_payslips_tenant_emp_period`/`_tenant_run`, `idx_workflow_executions_tenant_status`.
