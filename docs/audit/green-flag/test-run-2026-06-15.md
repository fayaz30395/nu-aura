# NU-AURA Test Run — 2026-06-15

**Trigger:** Post-commit verification for fca3178b (RBAC-7 and RBAC-8 fixes)
**Date:** 2026-06-15
**Scope:** Backend unit tests (no Docker/Testcontainers), Frontend TypeScript typecheck

---

## Backend Test Results

**Command:** `mvn test -pl backend -am --no-transfer-progress -Dtest="!*IntegrationTest,!*IT"`

| Metric | Count |
|--------|-------|
| Tests run (Maven summary) | **3,931** |
| Failures | **0** |
| Errors | **0** |
| Skipped | **0** |
| Build result | **SUCCESS** |
| Duration | 6m 22s |

> Note: Surefire report total shows 4,043 across all `.txt` files (including nested test class reports that Maven aggregates differently). Zero failures and zero errors in both counts.

### Prior baseline comparison

- Prior green baseline (2026-06-09): **4,029 tests** via Testcontainers (full integration suite)
- Prior unit-only run (2026-06-15): **3,931 tests** (same exclusion filter, no Docker)
- Delta explanation: ~98 tests are Testcontainers-backed integration tests excluded by the `!*IntegrationTest,!*IT` filter

**No regressions detected.**

### Specifically verified test classes (modified files in git status)

| Test Class | Tests Run | Failures | Errors |
|------------|-----------|----------|--------|
| `TenantProvisioningServiceTest` | 2 | 0 | 0 |
| `WorkflowServiceAutoDelegationTest` | 6 | 0 | 0 |
| `WorkflowServiceTest` | 0 | 0 | 0 |

All three test classes in the uncommitted diff ran cleanly.

### Architecture guards

| Guard | Result |
|-------|--------|
| `TenantTimeArchitectureTest.production_code_should_not_call_unzoned_now` | PASS (8.748s) |
| `TenantTimeArchitectureTest.production_code_should_not_hardcode_zone_ids` | PASS |

---

## Frontend TypeScript Check

**Command:** `npx tsc --noEmit` (from `frontend/`)

| Result | |
|--------|-|
| Exit code | **0** |
| Errors | **0** |
| Status | **CLEAN** |

---

## Verdict

**PASS**

- 3,931 backend unit tests: 0 failures, 0 errors
- 0 TypeScript compilation errors
- Both modified test files (TenantProvisioningServiceTest, WorkflowServiceAutoDelegationTest) run green
- Architecture guards (timezone discipline) still enforced
- No regressions introduced by RBAC-7 / RBAC-8 fixes in fca3178b
