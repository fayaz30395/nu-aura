# Release Readiness Report (REL-01, Wave 2)

**Agent:** release · **Date:** 2026-06-10 · **Scope:** migration chain, build verification, prod config, monitoring, rollback, smoke checklist.

## Findings

| ID | Severity | Module | Description | Impact | Exact Fix | Owner Agent | Status |
|----|----------|--------|-------------|--------|-----------|-------------|--------|
| REL-1 | INFO | db/flyway | Migration chain verified: V283__payroll_run_period_unique.sql + V284__workflow_optimistic_locking.sql exist in backend/src/main/resources/db/migration, valid PostgreSQL, no version collision (V272→V277→V282→V283→V284; `sort -V | uniq -d` clean). Only V283/V284 are new (git untracked); no other new migrations appeared. | None — verification pass | None | release | Closed |
| REL-2 | INFO | db/flyway | V283 dedup logic verified SAFE: soft-delete only (is_deleted/deleted_at — deleted_at added to payroll_runs by V51); survivor ranking matches PayrollRun.PayrollStatus enum exactly (DRAFT/PROCESSING/PROCESSED/APPROVED/LOCKED); deterministic tiebreak (created_at, id); partial unique index `WHERE is_deleted=false` permits period re-creation; DROP/CREATE use IF EXISTS guards. | None | None | release | Closed |
| REL-3 | MEDIUM | db/flyway | V283 side effect: payslips belonging to soft-deleted duplicate runs remain is_deleted=false → a period could expose payslips from two runs in payslip queries that don't join through active runs. | Possible duplicate payslip rows per employee/period in list/export paths | Post-deploy data check: soft-delete payslips whose payroll_run is soft-deleted, or verify all payslip queries join payroll_runs.is_deleted=false | dev | Open |
| REL-4 | HIGH | build/frontend | `npx tsc --noEmit` BLOCKED-NEEDS-NATIVE-RUN: two sandbox attempts (40s/42s timeouts) produced zero output — mounted-FS sandbox cannot type-check 4,059 TS files within the 45s exec cap. Evidence of a recent native run exists (frontend/tsconfig.tsbuildinfo written 2026-06-10 09:25, during the fix wave) but its exit status is unverifiable. | Green-flag criterion "tsc clean" unproven | USER/native: `cd frontend && npx tsc --noEmit` must exit 0; attach output to issue board | USER + release | Open |
| REL-5 | HIGH | build/backend | Backend compile BLOCKED-NEEDS-NATIVE-RUN: repo has NO maven wrapper (backend/mvnw absent; only start-backend.sh), sandbox has Java 11 only (pom requires `<java.version>21`), no mvn binary, no ~/.m2 cache → full dependency download + wrong JDK = infeasible in sandbox. | Green-flag criterion "backend build clean" unproven | USER/native: `cd backend && mvn -DskipTests compile` (Java 21) must exit 0 | USER + release | Open |
| REL-6 | LOW | build/repro | No mvnw/maven-wrapper committed — builds depend on host-installed Maven version; CI/agents cannot build hermetically. | Environment drift risk | `mvn wrapper:wrapper -Dmaven=3.9.9` and commit .mvn/ + mvnw | dev | Open |
| REL-7 | INFO | config | DEMO_CREDENTIALS_ENABLED defaults verified: base application.yml `false`, application-prod.yml `false`, application-render.yml unset (inherits false); only dev/demo profiles default true (acceptable). Closes the prod half of QA-1; local-testing half (flag=true locally) remains with USER. | None | None — keep env var UNSET in prod | release | Closed |
| REL-8 | INFO | config | Prod profile sane: ddl-auto=validate, flyway clean-disabled + no out-of-order/repair, swagger-ui disabled, error include-stacktrace/message=never, secure+__Host- cookies, CSRF on, captcha on, virus-scan fail-closed, rate limiting on, CORS env-driven (default explicit https://app.nulogic.io, no wildcard), SQL logging silenced, Redis TLS default on. No debug flags. | None | None | release | Closed |
| REL-9 | HIGH | deploy/k8s | Ingress hostname placeholders still present: helm values.yaml:134-135 (`hrms.example.com`, `api.hrms.example.com`) and raw infra/deployment/kubernetes/ingress.yaml:51-56,113. Wave-10 P1-4 partially remediated — helm NOTES.txt now warns at install, but no hard `fail` precondition, and raw manifests have only a TODO comment. | Deploying as-is yields dead ingress/managed-cert failure | Replace hosts with production domains at deploy time; optionally add `required`/`fail` guard in ingress.yaml template; raw k8s manifests need manual edit | USER + release | Open |
| REL-10 | INFO | monitoring | Alerting verified: infra/monitoring/prometheus/rules/ (hrms-alerts.yml, hrms-slo-alerts.yml) + alertmanager.yml with receivers. Payroll DLT covered: DeadLetterHandler @KafkaListener includes PAYROLL_PROCESSING_DLT (INT-1 fix landed) and pre-registers its `kafka_dlt_messages_total{topic=...}` counter, which KafkaDeadLetterReceived (15m) and KafkaDeadLetterAccumulating (1h>10) alerts watch. Payroll SLO alerts (duration>300s, failed-run) present. | None | None | release | Closed |
| REL-11 | INFO | monitoring | Actuator exposed but secured (SecurityConfig.java:216-220): /actuator/health permitAll (probes), /actuator/prometheus gated by PROMETHEUS_SCRAPE_TOKEN, all other /actuator/** require SUPER_ADMIN; health show-details=never in prod; liveness=ping only, readiness=ping,db,redis. | None | None | release | Closed |
| REL-12 | INFO | rollback | docs/runbooks/rollback.md exists and is current (triggers, kubectl undo, Cloud Build redeploy, PITR, secret rotation). Release-specific deltas (V283 forward-only/soft-delete down-strategy, V284 idempotent, image/config rollback, Kafka consumer-group notes) written to docs/audit/green-flag/rollback-plan.md. | None | None | release | Closed |
| REL-13 | CRITICAL | secrets | SEC-1 remains OPEN (carried): live Neon password + JWT secret + enc key in git history (d5961fef). Release readiness cannot be signed off until rotation is confirmed. | Token forgery + direct DB access | USER: rotate Neon pwd, JWT secret, enc key; purge history; add gitleaks to CI | USER | Open |
| REL-14 | HIGH | seed/qa | QA-2 carried: FINANCE_ADMIN role/user not seeded — payroll RBAC boundary untestable as designed; PROD-3 (LWF 501) descope decision pending; INT-3 (no outbox) accepted as documented known risk for this window. | Test coverage + statutory gap | Seed FINANCE_ADMIN; formally descope LWF + hide UI for IN release | release + USER | Open |

## Post-Deploy Smoke Checklist (10 items)

1. **Health**: `GET /actuator/health` → UP on every pod; readiness includes db+redis UP.
2. **Login**: SuperAdmin + a tenant HR_ADMIN + a plain EMPLOYEE log in via UI; `GET /me` returns correct roles; demo seed users (Welcome@123) REJECTED in prod.
3. **RBAC spot check**: EMPLOYEE cannot read another employee's `/leave-balances/employee/{id}` or `/benefits/.../employee/{id}` (RBAC-1/2 retest); SuperAdmin bypass still works everywhere.
4. **Leave apply**: EMPLOYEE applies 2-day leave → balance pending-reserved; invalid range (end<start) rejected 400 (DATA-1 retest); half-day on single day with period succeeds (BA-3 retest).
5. **Leave approve + reject**: manager approves one (balance deducted) and rejects another via workflow inbox (pending balance RELEASED — BA-2 retest).
6. **Payroll run view**: open payroll list and `/payroll/runs/[id]` detail page (PROD-1 retest — no 404); attempt creating a second run for the same period → blocked (V283/DATA-6 retest).
7. **Workflow concurrency**: double-click approve on one pending item → exactly one approval recorded, no duplicate callbacks (DEV-1/V284 retest).
8. **Migrations**: `SELECT version, success FROM flyway_schema_history ORDER BY installed_rank DESC LIMIT 5;` → V283, V284 success=true; duplicate-period query returns 0 rows.
9. **Monitoring**: Prometheus scrape of `/actuator/prometheus` succeeds with token; `kafka_dlt_messages_total` series visible for all 6 DLT topics; no DLT alert firing.
10. **Terminated-user lockout**: terminate a test employee → their session/token can no longer authenticate (DATA-2 retest).

## Sign-Off

**CONDITIONAL-GO** — see final summary. Blocking conditions: native `tsc --noEmit` + backend compile evidence (REL-4/5), SEC-1 secret rotation (REL-13), ingress hostnames replaced at deploy (REL-9), UI retest wave for the 25 Retest items (ENV-1 dependency).
