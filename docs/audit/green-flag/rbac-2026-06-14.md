# RBAC Audit — Green-Flag Run 2026-06-14

**Agent:** rbac · **Repo:** /Users/fayaz.m/IdeaProjects/nulogic/nu-aura · **Scope:** IDOR sweep, permission-literal mismatches, missing-authorization mutations/PII, privilege-escalation paths, SuperAdmin-bypass centralization.

**Method:** Compared every `*_VIEW_SELF` / `*_SELF`-gated controller endpoint taking an `{id}`/`{employeeId}` path var against the canonical ownership-enforcement pattern in `EmployeeController.enforceEmployeeViewScope` (EmployeeController.java:191-230). Verified RBAC-1/2/3/4 prior fixes still hold. Traced service-layer scoping for each suspect.

---

## Prior-finding regression check (all still FIXED)

| Prior | Status | Evidence |
|-------|--------|----------|
| RBAC-1 (benefits `/employee/{id}` IDOR) | STILL FIXED | All `/employee/{employeeId}` GETs call `enforceBenefitViewScope` — BenefitEnhancedController.java:198,207,291,317,326,344 |
| RBAC-2 (leave-balances `/employee/{id}` IDOR) | STILL FIXED | `enforceLeaveBalanceViewScope` on both endpoints — LeaveBalanceController.java:81,99 |
| RBAC-3 (STATUTORY underscore literals) | STILL FIXED | All statutory controllers use `Permission.STATUTORY_VIEW`/`STATUTORY_MANAGE` constants → resolve to colon form `STATUTORY:VIEW`/`STATUTORY:MANAGE` (Permission.java:185-186) |
| RBAC-4 (claim getClaim/appealClaim/submitClaim IDOR) | STILL FIXED | `enforceBenefitViewScope`/`enforceClaimWriteScope` on getClaim:234, appealClaim:281; submit at service layer |

**SuperAdmin bypass — centralized, by design (not a finding):** single implementation in `PermissionAspect.checkPermission` (PermissionAspect.java:82-90), evaluated before any permission check, with an INFO-level audit log line on every bypass. Per-controller scope helpers also short-circuit on `SecurityContext.isSuperAdmin()` defensively, but the authoritative gate is the aspect. No duplicated or bypassable path found.

**Privilege-escalation surface — clean:** all role/permission mutations require `ROLE_MANAGE` with `revalidate=true` (fresh DB lookup, defeats stale-JWT escalation) — RoleController.java:46,53,62,69,78,87,100,112. User role assignment requires `USER_MANAGE` — UserController.java:53. `/users/me` and `/employees/me` return only the caller's own record resolved from the JWT; no self-elevation path. No non-admin endpoint can grant roles/permissions or mutate another user's role.

---

## NEW Findings

| ID | Severity | Module | Description | Impact | Exact Fix (file:line) | Owner Agent | Status |
|----|----------|--------|-------------|--------|-----------------------|-------------|--------|
| RBAC-5 | HIGH | employee/skills | `EmployeeSkillController.getEmployeeSkills` (`GET /api/v1/employees/{employeeId}/skills`) is gated only by `EMPLOYEE_VIEW_SELF` and passes `employeeId` straight through; `SkillService.getEmployeeSkills` scopes by `tenantId` only — no self/team ownership check. Same IDOR class as RBAC-1. | Any authenticated employee reads any colleague's full competency/skill profile by guessing/enumerating their employeeId | Add `enforceEmployeeViewScope(employeeId)` (mirror EmployeeController.java:191) in EmployeeSkillController.java:48 before the service call, OR add a self/privileged scope guard in SkillService.getEmployeeSkills (SkillService.java:23-25). Controller-level guard preferred for consistency with benefits/leave. | fix-rbac | Open |
| RBAC-6 | HIGH | analytics/dashboard | `DashboardsController.getEmployeeDashboardById` (`GET /api/v1/dashboards/employee/{employeeId}`) is gated by `EMPLOYEE_VIEW_TEAM` but neither the controller nor `EmployeeDashboardService.getEmployeeDashboard(UUID)` enforces the team/reportee boundary — service scopes by `tenantId` only. | Any holder of EMPLOYEE_VIEW_TEAM (MANAGER/TEAM_LEAD) pulls ANY tenant employee's full personal dashboard — attendance, leave, payroll figures, performance, career — not just their reportees. Cross-employee PII + payroll exposure. | Add a team-scope guard: in DashboardsController.java:158 call a check that `SecurityContext.getAllReporteeIds().contains(employeeId)` (allowing VIEW_ALL / admin bypass), mirroring `enforceLeaveBalanceViewScope` VIEW_TEAM branch (LeaveBalanceController.java:59-63). Alternatively enforce in EmployeeDashboardService.getEmployeeDashboard (EmployeeDashboardService.java:72). | fix-rbac | Open |
| RBAC-7 | MEDIUM | probation | `ProbationController.acknowledgeEvaluation` (`POST /api/v1/probation/evaluations/{evaluationId}/acknowledge`) is a mutation gated by `EMPLOYEE_VIEW_SELF`; `ProbationService.acknowledgeEvaluation` scopes by `tenantId` only and never checks the evaluation belongs to the current employee. | Any employee can acknowledge (and inject `employeeComments` into) another employee's probation evaluation by guessing evaluationId — tampers with another's probation record | In ProbationService.acknowledgeEvaluation (ProbationService.java:335-340), after loading the evaluation resolve its owning employeeId and assert it equals `SecurityContext.getCurrentEmployeeId()` (allow SuperAdmin/HR bypass), throwing AccessDeniedException otherwise. | fix-rbac | Open |
| RBAC-8 | MEDIUM | survey | `SurveyAnalyticsService.submitResponse` (called from `SurveyAnalyticsController.submitResponse`, `EMPLOYEE_VIEW_SELF`-gated) trusts `request.getEmployeeId()` from the client body instead of resolving it from `SecurityContext` (contrast PulseSurveyController.submitSurvey which uses `getCurrentEmployeeId()` — PulseSurveyController.java:211). | A caller can attribute a survey response to an arbitrary employeeId (response spoofing / poisoning engagement analytics, defeating the duplicate-submit guard at SurveyAnalyticsService.java:99-101). | In SurveyAnalyticsService.submitResponse (SurveyAnalyticsService.java:91-99) ignore the client-supplied employeeId for non-anonymous surveys and set it from `SecurityContext.getCurrentEmployeeId()`; for anonymous surveys leave null. | fix-rbac | Open |

---

## Confirmed-safe (checked, no action)

- `PayrollController` payslip/salary-structure `/employee/{employeeId}` endpoints — all call `validateEmployeeAccess(employeeId, determineViewPermission())` with a PAYROLL_VIEW_SELF self-equality branch (PayrollController.java:283-451,598-612).
- `AssetManagementController.getAssetsByEmployee` (`EMPLOYEE_VIEW_SELF`, `{employeeId}`) — service enforces self-or-privileged scope (AssetManagementService.java:238-245, "BOLA FIX" present). Safe.
- `PulseSurveyController` start/submit — employeeId from SecurityContext, not path/body. Safe.
- `DashboardsController.getEmployeeDashboard` (self) — employeeId from SecurityContext. Safe.
- No `@PreAuthorize` / `hasAuthority(...)` / raw-string `@RequiresPermission("…")` literals exist in any controller — every gate uses `Permission.*` constants, so the underscore-vs-colon mismatch class (RBAC-3) cannot recur via literals. No empty/missing `@RequiresPermission` on data-mutating or PII endpoints outside the intentional, documented exceptions (`/users/me`, `/auth/**`, webhooks, career-page, preboarding/exit token pages); the empty-annotation case fails closed at PermissionAspect.java:95-99.
